# Group Support E10 QA Log — 2026-03-07

Scope:
- Phase E Milestone E10 polish + regression closure for group support changes.

## 1) UX Polish Coverage

Completed:
- Group empty state text for new group conversations in `assets/js/chat.js`.
- Member-add success/error inline feedback via composer status + modal messaging.
- Lightweight `new` badge for updated group chats in sidebar list.
- Group panel accessibility improvements:
  - region/list semantics in `dashboard.php`,
  - `aria-expanded` sync on group info toggle,
  - action button `aria-label` for remove/transfer controls.

## 2) Regression Checks Executed

### Diagnostics
- VS Code diagnostics (`get_errors`) run on updated files:
  - `dashboard.php`
  - `assets/js/chat.js`
  - `assets/css/dashboard.css`
- Result: no errors.

### Backend Syntax Validation
- Command:
  - `find api includes -name '*.php' -print0 | xargs -0 -n1 php -l`
- Result: no syntax errors across all API/include PHP files.

## 3) Direct Chat Safety Review (Code-path)

Reviewed critical direct-chat paths for regressions after group updates:
- send text (direct)
- fetch/poll direct messages
- seen-status polling for direct chats
- reply/forward/copy/details actions in direct mode
- file/image/voice direct upload guards

Result:
- direct chat behavior remains compatible,
- group-specific branches are gated by target type (`group:*`).

## 4) Follow-up Before Production Release

Recommended manual browser smoke in a real session (owner/operator run):
- direct chat send/receive/reply/forward/media,
- group create/join/add/remove/leave/transfer,
- mobile long-press context menu and accessibility keyboard checks.

## 5) Post-E10 Security/Robustness Hardening (Review Pass)

Applied after full review:
- invite link generation now respects subdirectory deployment paths (`groupBuildJoinLink`),
- unique join token generation now checks collisions (`groupGenerateUniqueJoinToken`),
- join token validation tightened for format/length in `join_group.php`,
- ownership transfer hardened with transaction locks/rowcount checks,
- owner leave flow hardened with member-row locking to prevent race inconsistencies,
- pagination overflow guard added for `fetch_messages.php` when client offset exceeds total.

Validation:
- diagnostics clean on modified files,
- full PHP lint pass across `api/` + `includes/` remains clean.

## 6) Legacy Group Encryption QA Checklist (Text)

Goal:
- validate older groups (created before group text encryption rollout) remain readable,
- verify new text messages are encrypted with `gcm1` format,
- identify any member-key assignment gaps.

Checklist:
- [ ] Run DB migration `migrations/08_add_group_text_encryption.sql` in target environment.
- [ ] Run DB migration `migrations/09_expand_message_text_capacity.sql` in target environment.
- [ ] Login as admin and call `api/group_key_health.php` with POST + CSRF.
- [ ] Confirm `missing_member_keys_count = 0` for all active groups.
- [ ] For legacy groups with old history, confirm `legacy_plaintext_text_messages_count > 0` may exist (expected for old rows).
- [ ] Send a **new** text message to each tested legacy group and confirm counter `encrypted_text_messages_count` increments.
- [ ] Add a new member to an old group, reload as that member, and verify they can decrypt/read **newly sent** group text.
- [ ] Join a group via invite link and verify the joiner can decrypt/read newly sent group text.
- [ ] Remove and re-add a member, then verify decryption still works for new group text.
- [ ] Forward text `group -> group` and `user -> group`, then verify target group members can decrypt the forwarded text.

Pass Criteria:
- all active group members have wrapped keys (`missing_member_keys_count = 0`),
- all new group text rows are encrypted (`message` starts with `gcm1:`),
- no decryption errors in UI for tested members and forward targets.
